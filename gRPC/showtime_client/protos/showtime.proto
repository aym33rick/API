syntax = "proto3";

service Showtime {
  rpc GetListShowtimes(Empty) returns (stream ShowtimeData) {}
  rpc GetTimesByDate(Date) returns (ShowtimeData) {}
}

message Date {
  string date = 1;
}

message ShowtimeData {
  string date = 1;
  repeated string movies = 2;
}

message Empty {
}